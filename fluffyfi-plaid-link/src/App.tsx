import React from 'react';
import { Box, Button, Container } from '@mui/material'

function App() {
  return (
    <Box display={"flex"} justifyContent={"center"}>
        <Button fullWidth size="small" variant="contained">Connect</Button>
    </Box>
  );
}

export default App;
